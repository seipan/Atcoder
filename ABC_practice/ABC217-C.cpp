#include<bits/stdc++.h>
using namespace std;

int main(){
    int N;cin >> N;
    vector<int> p(N);
    vector<int> v(N);
    for(int i=0;i<N;i++) cin >> p[i];
    for(int i=0;i<N;i++) v.push_bak(i+1);

    sort(v.begin(),v.end(), [&](int a,int b){
        
    }
}