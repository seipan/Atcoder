#include <bits/stdc++.h>
using namespace std;

int main(){
    int N;
    cin >> N;
    vector<int> L(N);
    vector<vector<int>> data(N, vector<int>(N));

    for (int i = 0; i < N; i++) {
        cin >> L[i];
        for (int j = 0; j < L[i]; j++) {
            cin >> data.at(i).at(j);
        }
    }

    for(int i=0;i<N;i++){
        for(int j=0;j<L[i];j++){
            if(L[])
        }
    }
}