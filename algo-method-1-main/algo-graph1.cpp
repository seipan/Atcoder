#include <bits/stdc++.h>
using namespace std;

int main(){
    int N,A,B; cin >> N >> A >> B;
    vector<string> A(N);
    for(int i=0;i<N;i++) cin >> S[i];
    bool flag=false;
    if(S[A].at(B)=='o' && S[B].at(A)=='o')flag = true;

    if(flag) cout << "Yes" << endl;
    else cout << "No" << endl;

}